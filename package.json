{
  "name": "loup-garou",
  "private": true,
  "version": "0.1.0",
  "description": "Production-quality companion app for the Loup-Garou party game",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/server & npm run dev --workspace=apps/web",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/game-engine && npm run build --workspace=packages/rating && npm run build --workspace=apps/server && npm run build --workspace=apps/web",
    "build:packages": "npm run build --workspace=packages/shared && npm run build --workspace=packages/game-engine && npm run build --workspace=packages/rating",
    "test": "npm run test --workspace=packages/game-engine && npm run test --workspace=packages/rating && npm run test --workspace=apps/server",
    "test:watch": "npm run test:watch --workspace=packages/game-engine",
    "lint": "npm run lint --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate --workspace=apps/server",
    "prisma:migrate": "npm run prisma:migrate --workspace=apps/server",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
