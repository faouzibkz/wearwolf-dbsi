node_modules/
dist/
build/
.next/
out/
*.log
npm-debug.log*
# Persistent action log (apps/server/src/logging/actionLog.ts) — real
# player/game data from real game nights, bind-mounted from docker-compose's
# `server` service into ./logs at the repo root. Never committed.
/logs/
.env
.env.local
.env.*.local
.DS_Store
coverage/
.turbo/
apps/server/prisma/dev.db
apps/server/generated/
*.tsbuildinfo
.deploy-marker
infra/aws/images-*.json
# deploy-cloudrun.ps1's own local state — a DB password in plaintext and a
# raw pg_dump of real account/ranking data must never be committed.
.cloudsql-db-password.txt
.cloudsql-migrated
loupgarou-dump.sql
